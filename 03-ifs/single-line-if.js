

/** 
if (true) {
    ...
} else if (...) {
    ...
} else {
    ...
}

*/

// ifs that return a value

// single line if

const hello = Math.random() > 0.5 ? 'high' : 'low';

// if ("sdfsdf") {

// }

// truthy => "ddsfd"
// falsey => "", false, null, undefined
const defaultValue = window.stam || 'default value'
const defaultValue1 = window.stam ? window.stam : null;


const anotherValue = 'default value' && window.hello ;
const anotherValue1 = 'defaultValue' ? window.hello : 'default value';



